
import { IUserDisplay } from '../../../../models/contracts/request/user/iuserdisplay';

export class MilestoneAddCtrl {

    static $inject = ['Injected']
    constructor(private Injected: IUserDisplay) {
        
    }

}